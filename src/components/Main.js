import React from 'react'
import Equation from './Equation'

export default function Main() {
  return (
    // if view = equation => Equation
    //if view = timer, => timer
    // if view = score => score
    
  )
}
